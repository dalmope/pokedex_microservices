server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost/pokedex
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

appDescription: "Pokedex API"
appVersion: "1.0.0"